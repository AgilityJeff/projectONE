<?php
// Functions ==========================================================================================================
	// Validate the incoming $_POST data
		function validate($Data) {
			return TRUE;
		}
		
	// Process a validated submission
		function process($Data, $id="") {
			return TRUE;
		}
		
	// Retrieve a record and send back the array of values for defaults or send back blank array for a new record
		function prepare($id="") {
			return FALSE;
		}
		
// Procedure ==========================================================================================================
		
?>